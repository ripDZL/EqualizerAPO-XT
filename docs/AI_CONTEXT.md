# AI Context

- 2026-09-04 v2.51.0 stamp revalidation: local v143 rebuild stamps both Editor and universal Setup `2.51.0.0`; workflow Pester remains 126/126, VST and synthetic VST3-panel self-tests pass, core tests remain EditorLogic 4,585 / HybridConv 1,635 / Engine 1,320 / Asio 447, and fake-ASIO gate passes 9/9. This is local preflight only; exact v145 CI, missing AudioRegression reference fixtures, Theme Lab, and user install testing remain separate gates.

- 2026-09-03 upstream `v2.51.0` candidate: isolated `codex/upstream-2510-integration` merges tagged upstream `3e595dc8` plus its release stamp `4c28c2cf` onto `origin/beta` `3b6657c5`; post-tag diagnostic `74fd96ec` is intentionally excluded. Conflict resolution preserves the fork VST preview/Legacy/Clarity behavior while adding upstream’s Minimal vertical register-drum control and private-font cleanup. Local preflight: workflow Pester 126/126; EditorLogic 4,585; HybridConv 1,635; Engine 1,320; Asio 447; Qt Editor VST and VST3-panel self-tests; 5 bases/22 themes; and inspected dark/light knob specimens. Exact CI v145 is absent on this host, so native/Qt builds use v143 preflight only. AudioRegression lacks its committed reference fixtures (do not regenerate them); Theme Lab is not rerun and skin-switch only has a non-crash smoke, not an exit-code pass. No beta/main/release/install/audio-service change. Next: explicit approval for candidate install-test, then normal beta fast-forward and CI only if accepted.

- 2026-09-01 upstream `v2.50.7` candidate: isolated `codex/upstream-2507-integration` merges `upstream/main` `c56a0484` (tag `56c10764`) onto `origin/beta` `f5fd5041`, preserving fork `DistributionConfig` update routing and fork Clarity/Graphite release notes. Green locally: source/variant/skin checks; x64 native; EditorLogic 4,574; HybridConv 1,635; Engine 1,320; Asio 447; AudioRegression 31/31; ASIO probe 9/9; Qt Editor build; VST and VST3-panel self-tests. Theme Lab's offscreen command exceeded a 120-second local bound; its logic is covered by EditorLogic and no theme code changed. Next: normal beta fast-forward/Actions, then install-test; do not touch `main` or release yet.

- 2026-08-31 beta `v2.50.6-beta.1` is public from green Actions run `33404008513` / exact commit `43926271`. It is a published prerelease (not draft/stable) with 33 assets: six Setup/MSI/feed/full/delta sets, beta-pinned universal Setup, source archive, SHA256SUMS, and generated notes. All 13 installer hashes match GitHub asset digests; `Publish-Release.ps1` is complete; safe `--detect-only` selects the beta-pinned AVX-512 MSI. `main` remains `5c49de19`; next gate is user beta install-testing before any explicit main promotion.

- 2026-08-31 local v2.50.6 AVX-512 test install: the 69-file release-layout payload from `24c6a7dd` is hash-verified in `C:\Program Files\EqualizerAPO-XT-x64-avx512\current`. Backup: `artifacts\install-backups\upstream-v2506-local-avx512-20260831-100454`. The install retained `config`, updater metadata, and documentation shortcuts; Windows Audio was stopped/restored; no reboot occurred. The user accepted the test and authorized a normal beta fast-forward; await beta Actions before any release or main promotion.

- 2026-08-31 upstream `v2.50.6` candidate: local merge `b45db4d6` brings `upstream/main` `40fb6ccf` onto `origin/beta` `44ac01d7`, retaining the fork VST preview/UI/installer behavior. Green locally: source/variant/skin-module checks; x64 native build; EditorLogic 4,574; HybridConv 1,635; Engine 1,320; Asio 437; AudioRegression 31/31; Qt Editor/DeviceSelector/UpdateChecker; VST self-test; VST3 Open Panel; Theme Lab 44/0; ASIO probe 8/8; power/routing/card/skin gates; 6,644-shot gallery and 12-shot Legacy readability gallery. No remote, release, installer, runtime, or audio-service change. Next: explicit approval for beta push/CI, then user install-test before any `main` promotion.

- 2026-08-30 published beta `v2.50.2-beta.1` from green Actions run `33327886701` / exact beta commit `2ea7bcbd`. The public release is prerelease-only (not draft/stable) with 33 assets: six Setup/MSI/feed/full/delta sets, universal Setup, source, SHA256SUMS, and generated notes. All 13 installer hashes match GitHub asset digests; `Publish-Release.ps1` is complete. The verified CI AVX-512 payload is installed at `C:\Program Files\EqualizerAPO-XT-x64-avx512\current` with configuration preserved and a pre-install backup in `%LOCALAPPDATA%\Temp\EAPOVST364bit-v2502-beta-33327886701\preinstall-backup`. `main` remains `5c49de19`; next gate is user beta testing before any explicit main promotion.

- 2026-08-30 v2.50.2 integration candidate: merged upstream `284fc5a2` as `d59705d4`, then `320a9e84` registers x64 endpoint ASIO entries for the shipped Win32 wrapper while ARM64 remains native-only. Green: Pester 126/126; EditorLogic 4,572; HybridConv 1,635; Engine 1,313; Asio 431; AudioRegression 31/31; ASIO probe 8/8; VST self-test; skin galleries 6,644 + Legacy readability 12; x64/Win32 ASIO wrapper and qmake DeviceSelector builds. Candidate normally fast-forwards `origin/beta`; `main`, releases, installer, runtime, and audio services remain untouched.

- 2026-08-29 user-approved beta prerelease: `v2.49.0-beta.1` is public from `bd92a2de` after green Actions run `33280780780`. It is a published prerelease (not draft/stable) with 33 assets: six complete channel package sets, the beta-pinned universal Setup, exact source archive, SHA256SUMS, and generated notes. `Publish-Release.ps1` reports complete; `main` remains unchanged.

- 2026-08-29 v2.49.0 integration candidate: `codex/upstream-2490-integration` merges upstream `4acab38b` (released `v2.49.0`) as `b6a58e0b`. Local repairs keep large Editor/DeviceSelector Qt resource bundles in two-step mode and include every shared skin-QSS icon in DeviceSelector. Local validation is green: Pester 126/126; EditorLogic 4,572; HybridConv 1,635; EngineOrchestration 1,292; Asio 385; AudioRegression 31/31; VST self-test; ASIO fake probe 8/8; Theme Lab 44/0; skin/routing/card-move and 225-shot DeviceSelector galleries. Candidate is ready only for a normal beta fast-forward; `main`, releases, installer, active runtime, and audio services remain untouched.

- 2026-08-29 stable `v2.47.1` is published from `eee86aa516e325431600d759e7218e73db2050fa` after green Actions run `33230775759`. It is public (not draft/prerelease) with 33 assets: all six channel Setup/MSI/feed sets, universal Setup, source archive, SHA256SUMS, and generated notes. `Publish-Release.ps1` reports complete with no missing channels or follow-up assets. Keep `beta` as a non-force fast-forward of post-release `main`.

- 2026-08-29 stable-release gate repair: main run `33230056711` was green but skipped build/release because `v2.47.1-beta.1` and `version.h` share `2.47.1`. The gate now emits `release_required=true` for that untagged same-version prerelease case; workflow logic builds/releases without rewriting `version.h`. Pester 5 (25 focused checks) and an origin-only dry run pass. Replacement main run `33230775759` is green and published stable `v2.47.1`.

- 2026-08-29 user-approved beta prerelease: published `v2.47.1-beta.1` from green beta commit `4ce8efdf` / Actions run `33220187263`. The complete 32-asset prerelease includes all six channel packages, universal Setup, source snapshot, checksums, and generated notes; workflow-equivalent completeness is green. `main` remained untouched at prerelease publication; later user approval promoted the same release line to stable `v2.47.1`.

- 2026-08-28 user-approved beta push: fast-forwarded `origin/beta` `e9fadd7a..f5f0ebde` with `fc894cee` (group Clarity themes), `aaca996` (menu/gallery regression), and the test-install handoff. The user reports the installed candidate looks good. `main`, releases, installers, engine, and audio services remain unchanged. Next: beta CI before any release or `main` promotion.

- 2026-08-28 user-approved Clarity-menu test install: overlaid only `C:\Program Files\EqualizerAPO-XT-x64-avx512\current\Editor.exe` from local `aaca996` (v2.47.1.0). Prior `1069728D7657A75EF4F4BEB37DAB56D1345B6659CF8FA610E619D330EFFE7147` is backed up at `artifacts\install-backups\clarity-menu-avx512-20260828-185900\Editor.exe`; installed `6A5255E70E0B0485D26869E30A23842E58D4757081220C45ACD61CBA055A2B59` matches the candidate. Engine, configuration, plug-ins, and audio services are unchanged. Manual gate: View > Interface shows Clarity High Contrast directly above Graphite Clarity and both still apply. No remote or release changed.

- 2026-08-28 local-only Clarity-menu follow-up: View > Interface now places `Graphite Clarity` immediately after `Clarity High Contrast` without changing stable roster ids or their shortcut ordinals. `EditorLogicTests` passes 4,572 checks; Release AVX-512 Editor builds; new `thememenu` gallery captures Clarity/Graphite in light/dark, and the 12-shot Legacy readability gallery remains green. Evidence is under ignored `artifacts\clarity-menu-gallery-20260828` and `artifacts\clarity-legacy-gallery-20260828`. No remote, release, installer, or installed runtime changed. Next: user decide whether to push/build/install this small follow-up.

- 2026-08-28 beta test install: GitHub Actions run `33214676823` is green for beta `2d91bd6d` (v2.47.1) with all six variants, Pester, cppcheck, memcheck, UI gates, packaging, and cross-variant comparison; release creation was skipped by the beta-only rule. User approved a full AVX-512 overlay at `C:\Program Files\EqualizerAPO-XT-x64-avx512\current` after a 75-file backup at `artifacts\install-backups\beta-v2471-avx512-20260828-182654`. Windows Audio was paused/restored, all 64 artifact files hash-verified, and Editor/engine report `2.47.1.0` (`1069728D...EFFE7147` / `ABCC2976...701615B9`). Editor is running. `main` and releases remain unchanged. Next: user test, then decide on release/main promotion.

- 2026-08-28 user-approved beta promotion: fast-forwarded `origin/beta` from `0523964a` to `242a3f86` (the validated v2.47.1 integration). It contains upstream `df26f9da` plus the fork beta history; `main`, releases, installers, and installed files remain unchanged. All 18 conflicts are resolved in merge `e94458fc`. Local passes: EditorLogic 4,548; HybridConv 1,635; EngineOrchestration 1,259; Release AVX-512 Editor; Device Selector Release; core EqualizerAPO Release; skin-module gate (5 bases/22 themes); Theme Lab (44/0); skin switch (132/198, worst 873 ms); Legacy Clarity/Graphite gallery (12); VST-aware gallery (6,600). Selected endpoint -> fork preview; no endpoint -> upstream panel monitor; Bertom native -> neither. AudioRegression lacks reference fixtures; local Pester 3.4 cannot run Pester-5 scripts; Installer compilation remains unrun. Next: verify beta CI before any release or `main` promotion.

- 2026-08-28 upstream pre-push audit: fetched `115dkk/EqualizerAPO-XT`; `upstream/main` is `df26f9da` at released `v2.47.1`, while the prepared local `beta` is `ef85c9be`. They diverge from `v2.42.2` (`5524cf25`): 138 fork-only and 41 upstream-only commits, with 44 changed-file overlaps. A non-checkout merge preflight reports 18 conflicts in VST panel/picker code, galleries, light QSS, translations, release notes, tests, and `version.h`. Upstream adds its own panel feed/monitor, VST3 picker, type-scale, brightness, and Minimal-skin work. Beta push is paused pending an explicit decision to integrate upstream first; no worktree, branch, remote, install, or release was changed by the audit.

- 2026-08-27 Graphite Clarity: added built-in `graphite`, a charcoal dark/light high-readability theme with a square dense instrument form (6px signal rail, wire plates, gradient scope bars, 48px solid rows) distinct from Clarity. Primary/secondary contrast is pinned on normal, hover, and selected cards. Legacy VST3 channel fill now wraps into three-cell rows, preventing label/control overlap. Graphite appends to the roster so existing Ctrl+Alt theme shortcuts remain stable. The required Legacy Rows gallery covers `clarity,graphite` in dark/light active/disabled states. Red/green `EditorLogicTests` passes 4,484 checks; Release AVX-512 Editor, `--theme-lab-test`, and fresh Modern/Legacy screenshot galleries pass. User-approved Editor-only overlay installed at `C:\Program Files\EqualizerAPO-XT-x64-avx512\current\Editor.exe`, SHA-256 `B528D825C13789A781ECE675AE7B642A6D1758765CCF4487919DE2828948165D`; prior Clarity-only Editor SHA-256 `D28EEB7F098A2C16A66E6FF5383484E6C6AF9CFD83641D9B40DC771AE7FFD287` is backed up at `artifacts\install-backups\graphite-clarity-avx512-20260827-232558\Editor.exe`. Engine, config, plug-ins, and audio services were unchanged. The user reports the installed candidate seems to work; the beta update is prepared locally but not pushed. Keep the four-combination test as the release-acceptance gate. A later `main` promotion will compute version `2.43.0`.

- 2026-08-27 Clarity High Contrast: added built-in `clarity`, an accessibility-first Modern/Legacy token variant with a dark/light pair, black-or-white labels, stronger outlines, focused states, and thick high-contrast knobs with adjacent values. Legacy Preamp now uses the shared bipolar `AudioKnob`, and CI has a dedicated required Legacy Clarity gallery. `EditorLogicTests` is green at 4,391 checks after a red roster/contrast test; Release Editor build, headless Theme Lab self-test, and fresh Modern/Legacy gallery renders pass. User-approved AVX-512 Editor-only overlay is installed at `C:\Program Files\EqualizerAPO-XT-x64-avx512\current\Editor.exe`, SHA-256 `D28EEB7F098A2C16A66E6FF5383484E6C6AF9CFD83641D9B40DC771AE7FFD287`; prior SHA-256 `3210E445FCC2FA44D3EBA6B0C4F25EAD20E159E7563AB47BA7FDC42614FCCAC3` is backed up at `artifacts\install-backups\clarity-high-contrast-avx512-20260827-150828\Editor.exe`. Engine, configuration, plug-ins, and audio services are unchanged. Manual gate: user-test all four combinations before beta push.

- 2026-08-27 branch policy (user): develop and test through `beta`; do not send ordinary work directly to `main`. After explicit approval, fast-forward `beta` into `main` for the stable release workflow. Keep `beta` aligned with the post-release mainline. The durable guide is `docs/BranchWorkflow.md`, linked from the README.

- 2026-08-27 stable `v2.42.5` is published from `4b723927514c3df28d1108fd9c22dc1c76cc9fca` with 33 assets: [release](https://github.com/ripDZL/EqualizerAPO-XT/releases/tag/v2.42.5), [run 33029195723](https://github.com/ripDZL/EqualizerAPO-XT/actions/runs/33029195723). All six variant builds, Pester, cppcheck, memcheck, cross-variant comparison, packaging, and release completeness pass. It contains the live VST-preview restoration and Program Files installer repairs. The local branch is fast-forwarded to the CI version-bump commit; the existing Program Files Editor remains the manually tested overlay, not a release reinstall.

- 2026-08-26 live-preview manual result: the user reports the installed AVX-512 candidate fixed the missing live VST preview. This confirms the reported normal separate-panel path; the Bertom popup exception remains deliberately unchanged and was not re-tested in this pass.

- 2026-08-26 installed live-preview candidate: user-approved Editor-only overlay in `C:\Program Files\EqualizerAPO-XT-x64-avx512\current\Editor.exe`; installed SHA-256 `8185116EFFEEB26B9B472550A6FCFD914172AF73ED5E7630A0D92BDB8717CF54`, prior SHA-256 `02AD1CB1840AA96E90EB25EA171B80E72577048FD7326C22E3093691FE727B1C`, backup `artifacts\install-backups\live-preview-popup-avx512-20260826-204754\Editor.exe`. Engine, configuration, plug-ins, and audio services are unchanged. Manual gate: normal VST `Open panel` live microphone response in modern cards and LegacyRows; verify Bertom still opens/closes safely.

- 2026-08-26 live VST-preview repair: the Denoiser popup crash fix had disabled the feed for every separate `Open panel` VST, so normal analyzers could no longer show microphone activity. `VSTPopupLivePreviewPolicy` restores the feed for normal native panels in both LegacyRows and modern cards, while keeping only `Bertom_DenoiserClassic.vst3` protected. The policy test was red at 1/4318 and green at 4319 checks; AVX-512 Editor 2.42.4.0 candidate SHA-256 `8185116EFFEEB26B9B472550A6FCFD914172AF73ED5E7630A0D92BDB8717CF54` is built but not installed. Manual gate: verify a normal VST panel responds to the active microphone, then verify Bertom still opens/closes safely (its separate panel intentionally has no live feed).

- 2026-08-26 local universal Setup candidate: `Installer\Release\EqualizerAPO-XT-Setup.exe` from `60f1742a` rebuilt as 2.42.4.0, SHA-256 `4B39DF838B213F639759C3767561B20DF3DB26C0D410F8BB8BC729E4E2C1CCBB`; safe `--detect-only` passes. It is ready for an explicit user-approved live test of the Program Files fallback only.

- 2026-08-26 universal Setup x86 repair: on this Windows build, the x86 front door receives `ERROR_FILE_NOT_FOUND` from `FOLDERID_ProgramFilesX64`, while native x64 and the protected 64-bit `HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\ProgramFilesDir` both resolve `C:\Program Files`. The installer now uses that native registry value only as a fallback; it never trusts a caller-controlled environment path. Red/green resolver coverage, an x86 `/reg:64` probe, Win32 Release Installer build, and EditorLogicTests 4313 pass. Next gate: package and user-test the universal Setup; preserve existing AppData and legacy installs.

- 2026-08-26 installer-scope repair: the local test helper selected the deliberately per-user channel Setup EXE, which created `C:\Users\Admin\AppData\Local\EqualizerAPO-XT-x64-avx512` at v2.42.4. The published universal Setup and MSI are machine-capable; `Install-ReleaseBuild.ps1` now defaults to the MSI, keeps `-InstallerKind PerUser` only for legacy CI repros, and targets `C:\Program Files\EqualizerAPO-XT-<channel>`. The universal front door uses the same flat target. Release-MSI metadata, installer build, and EditorLogicTests 4310 pass. Do not remove the existing per-user install; next gate is a user-approved live MSI install.

- 2026-08-25 Denoiser Classic popup-panel fix: live analyzer processing no longer starts for pop-out native VST panels, because it shares the controller instance and reproduced an access violation in Bertom Denoiser Classic. Embedded panels retain analyzer feed. `--selftest-vst3-panel` is an opt-in exact-path probe covering both LegacyRows and modern cards; the Denoiser raw VST3 module passes. Release Editor build, EditorLogic 4310, and HybridConv 1635 pass. User-approved Editor-only test build installed at `C:\Program Files\EqualizerAPO-XT-x64-avx512\current\Editor.exe` (SHA-256 `9B73921D47DD0449559B6D6C754D98CD565A8170416CD267CD8982AF67959200`); backup under `artifacts/install-backups/denoiser-popup-editor-avx512-20260825-173241`. Config, engine, and plug-ins untouched. On 2026-08-26, user confirmed panel open/close works in both LegacyRows and modern cards.

- 2026-08-25 beta prerelease: user-approved `v2.42.3-beta.1` is published from `28d868ae`; green GitHub Actions run `32857546525` covers Pester 5, cppcheck, memcheck, all six variants, UI gates, and cross-variant comparison. It contains the universal `EqualizerAPO-XT-Setup.exe`, all six per-machine MSIs/update feeds/full packages, source archive, and checksums; the release completeness gate passes. The universal beta Setup clean-rebuild/detect-only check resolves `x64-avx512` to the tag-pinned beta MSI. It targets `C:\Program Files\EqualizerAPO-XT\<channel>`, leaving legacy and existing per-user installs untouched. No runtime install was performed.

- 2026-08-24 active local test work: user confirms FabFilter Pro-Q now works; temporary probes/debug hook are removed. The initial split-menu repair was visibly too large (18px face/12px chevron), so it is now an 8px transparent separator with a 6px chevron for modern VST Browse and LegacyRows Options; legacy VST status colours stay tokenized. The new regression test fails on the oversized rule (120 failures) and passes at 4283 checks. Clean AVX-512 Editor hash `6B24EFD87C122F405F37DD31312D5681A4FB8FE7F0ADE89E869B21510C095210`; forced-offscreen gallery crashes locally, so the installed manual Copper Forge/Legacy Bronze light/dark check remains the gate.
- 2026-08-24 install: after the user closed Editor, overlaid only `current\Editor.exe` with the compact-arrow build and verified its hash. The replaced Editor is backed up at `artifacts\install-backups\compact-menu-arrow-avx512-20260824-192222\Editor.exe` (`F13921F032A5BCE8DA90A197944E1F1900E4EC20E65EF0023E219502219FE22E`). No engine or audio-service files changed.
- Local integration branch: `codex/upstream-289-port`.
- Remote branch: `origin/codex/upstream-289-port`; do not merge upstream or install from it without separate approval.
- Base: upstream `main` at `ff9c1747614546b68418a5376d0e5f893babd130` (v2.39.0).
- A direct merge from `beta` produced 149 conflicts; port fork-only features onto current upstream instead.
- Preserve upstream VST3 `Input`/`Output` bus-layout controls while porting VST preview work.
- VST2 no-native-editor safety is included as `767f3fdf`: an editor-less VST2 plug-in is rejected before any panel opcode is sent.
- Retained the legacy VST parameter parser for signed and leading-decimal values, plus the Visual Studio environment casing and clean-artifact packaging safeguards.
- Repaired the upstream `EditorLogicTests` project listing `AudioEngineAccess.cpp` twice; parallel compilation now has one object producer.
- Ported here: Editor-side VST live analyzer preview, microphone sources, selected-endpoint selection, and row-scoped `Device:` context.
- Ported VST2 host time/process-level safety with a deterministic host-callback regression test.
- VST preview processing is block-paced on a dedicated worker; shutdown joins it before the plug-in is stopped or released.
- Saved custom-theme persistence, JSON import/export, token overrides, Theme Lab UI, temporary live preview, and saved-theme application are ported.
- Review hardening keeps Theme Lab previews transient, synchronizes the menu state to a saved theme's stored dark setting, and preserves the Rack/Matrix base chrome for custom themes.
- Ported the first fork-only theme-variant group: Midnight Console, Arctic Bloom, Ember Rack, Violet Pulse, and Solar Paper. Each keeps its own token/id identity and reuses the appropriate upstream QSS and painter grammar.
- Ported the later dark group: Obsidian Glass, Aurora Veil, Copper Forge, Neon Nebula, and Noir Chrome. It reuses the same scoped QSS/painter grammar mapping without adding a new skin form.
- DeviceSelector resolves each variant to its `paintBaseId` for painted chrome while retaining the variant's QSS and tokens; no selector-specific variant painters were added.
- DeviceSelector now restores saved custom themes with their saved base grammar and token table; its temporary-settings coverage adds three custom-theme shots.
- Ported LegacyRows theming: legacy row factories and the stock font engine remain intact while shared chrome, row paint, dialogs, and token consumers use the selected palette.
- Added Legacy Slate, Blue, Forest, Bronze, and Plum as Minimal/Precision token variants for both LegacyRows and DeviceSelector.
- Ported direct VST3 bundle import: a dedicated VST3-folder picker reaches the bundle path without mutating the active reference on rejection; it stages the complete regular-file tree, requires the host-platform module, rejects reparse/traversal paths, and replaces prior bundles without stale members.
- Latest local proof: Editor Release build, offscreen VST self-test, 120-cycle skin-switch test, a full 5,840-shot 20-theme skin gallery, EditorLogic 3415, HybridConv 1635, a 30-shot LegacyRows gallery, DeviceSelector qmake/123-shot coverage, and the skin-module gate (5 base modules across 20 themes) all passed. The VST3 importer test covers tree copy, staged replacement, traversal rejection, non-VST3 directory rejection, platform-module validation, preservation of an existing bundle on invalid input, and VST picker reference preservation on an invalid folder. Representative LegacyRows and DeviceSelector captures were visually checked. The branch is pushed; no integration artifact is installed, merged, or promoted.
- The skin-module CI gate derives concrete module ownership from each roster entry's `paintBaseId`, while keeping all theme ids in its shared-code literal check; token variants intentionally do not own a `.pri` module.
- The gallery resolves expanded-dialog overflow expectations through `SkinManager::baseSkinId()`, so Studio-derived token variants retain Studio's intentional no-scroll contract. Its CI harness captures `qWarning` diagnostics.
- The local host has Pester 3.4 only; the repository's build-script tests require Pester 5 and remain a CI gate.
- 2026-08-24 theme pass: 20 themes × light/dark pass 4.5:1 text, muted, and selection contrast; token tooltip rule covers modern + LegacyRows.
- Theme Lab: picker/reset/repair, neutral live preview/audit, unsafe saved palettes fall back until repaired; `--theme-lab-test` is the widget gate.
- 2026-08-24 test install: commit `9f12df1c` Editor-only overlay hash-verified in registered AVX-512 runtime; backup `artifacts/install-backups/pre-theme-lab-9f12df1c-20260824-135508`; VST DLL + Program Files untouched.
- 2026-08-24 runtime diagnosis: `Editor.log` reproduces the exact FabFilter Pro-Q 4 raw-module rejection. Later PID attribution shows the relevant failure is Editor-side, not the active audio engine; the initial Editor-only theme overlay still predated the committed VST3 loader fix.
- 2026-08-24 installed candidate: rebuilt `Common` and `EqualizerAPO.dll` with `/arch:AVX512` from `9f12df1c`; v2.39.0 SHA-256 `3A11AB586EE7D5A2C3A6A3D87FC3744F1E501AEC82F48369FC9FA5B04A8949B7`. AVX-512 `HybridConvTests` passes 1,635 checks, including all 103 VST3 host checks. The prior engine (preflight v2.42.2, SHA-256 `52E96A...`) is backed up at `artifacts/install-backups/fabfilter-engine-avx512-20260824-144041`; after the brief Windows Audio restart, the new `audiodg` loaded the candidate hash.
- 2026-08-24 VST3 factory-host compatibility: accept `kNotImplemented` from optional `IPluginFactory3::setHostContext`; strict handling rejected real FabFilter/iZotope factories. Red/green coverage: 1 of 102 VST3 host checks failed before, then 103 VST3 host and 1,635 native checks passed. A 79/80 installed-VST3 sweep passed; the remaining old `win-rnnoise\\rnnoise.vst3` bundle is ACL-blocked (`LoadLibrary` access denied), while the configured newer RNNoise bundle passes.
- A hash-verified two-file AVX-512 test overlay (`Editor.exe`, `EqualizerAPO.dll`) is installed in the registered `...\\EqualizerAPO-XT-x64-avx512\\current` runtime; config and Program Files install were not changed. The fresh `Editor.exe --selftest-vst` timed out at 30 seconds and is not passing evidence. Manual gate: verify previously failing FabFilter/iZotope VST3 add/load/open behavior.
- 2026-08-24 follow-up: FabFilter Pro-Q 4's raw `.dll` is a VST3 module. The exact module, factory, component/controller instance, and stereo setup pass in the repaired loader probe. The AVX-512 test shortcuts had stale sandbox targets while the normal Configuration Editor shortcut remains the old Program Files build; only the two named AVX-512 test shortcuts now target the registered test runtime.
- LegacyRows regression: `CompactToolBar::sizeHint()` intentionally returns compact dimensions, but its vertical action stack was given only the first-action height. `updateMaximumHeight()` now sets the matching minimum height; the headless red/green toolbar probe changes from only Add visible to Add/Remove/Edit all visible. A hash-verified Editor-only overlay is installed; manual gate includes removing a row in LegacyRows.
- 2026-08-24 FabFilter diagnosis correction: the log's leading number is `GetCurrentThreadId`, not a PID; the error is from the Editor analysis thread. The exact `VSTPluginLibrary` loads Pro-Q 4 for 12 cycles, and the exact Editor FilterEngine probe loads the existing config successfully. Installed temporary diagnostic Editor `BCB4A1...EE26B` adds tagged failure-gate logs; backup is `artifacts/install-backups/fabfilter-editor-diagnostic-avx512-20260824-165026`. User UI retry is the current gate; remove diagnostics after result.
- 2026-08-24 v2.42.2 integration: merged exact upstream tag `5524cf25449c83d36a09e42afd119d49712943e6` locally, preserving fork VST preview/safety and theme work while adding upstream VST3 slot-fill UI. Resolved the gallery constructor argument conflict; Editor Release build, EditorLogicTests (4306), HybridConvTests (1635), and EngineOrchestrationTests (1259) pass. Upstream translation TS/QM files are retained; local `lupdate` cannot start because `Qt6Qml.dll` is absent. No v2.42.2 files are installed or pushed.
- v2.42.2 AVX-512 test candidate: `Editor.exe` `38E60FBD419364EC2E54FC1DF44EA1C720287FC439D823420557A33F6A7622CB`; `EqualizerAPO.dll` `F5A474AE444F3AF01E5CD439C8CBF11CB40C82418EC411B065817C22C15974CE`. A fresh AVX-512 HybridConvTests run passes 1635 checks. Await explicit install approval.
- 2026-08-24 user-approved v2.42.2 test install: overlaid only `current\\Editor.exe` and `current\\EqualizerAPO.dll`; backup `artifacts\\install-backups\\v2422-avx512-20260824-202257`. Installed hashes match the candidate. Windows Audio was stopped/restored for the locked engine DLL; the new `audiodg` has not yet loaded the APO, so real playback remains the acceptance proof.
- 2026-08-24 beta reconciliation published to `origin/beta` as `767178180cb0732cd9b2f61f66833afc90e0d150`, retaining its prior history. Ported VST3 full-bus/readback negotiation (inactive sidechain and adapted arrangements), legacy numeric `Input`/`Output` parser compatibility, and capture channel-mask selection. AVX-512 v143 validation: HybridConvTests 1,635 (VST3 host 124; parser 165), EngineOrchestrationTests 1,259, EqualizerAPO and Editor Release builds pass. No new runtime files are installed.
- 2026-08-25 beta prerelease: green GitHub Actions run `32795860636` built and validated all six variants at `7b2b349c`. Published `v2.42.2-beta.1` as a prerelease with hash-verified portable `x64-avx2` and `x64-avx512` ZIPs plus `SHA256SUMS.txt`; it is not an auto-updating stable release and no runtime files were changed.
- 2026-08-25 main promotion approved: `origin/main` (`4f10b939`) is an unprotected ancestor of `origin/beta`; promote by fast-forward only. This triggers the repository's stable release pipeline; no local runtime changes are authorized.
- 2026-08-25 release-gate repair: `v2.42.2-beta.1` was the nearest tag, so the original bump script saw only docs commits and skipped the stable build/release. `Bump-Version.ps1` now promotes a newer prerelease base before applying post-beta Conventional Commit bumps; its fixture regression covers docs-only promotion. An origin-only replay promotes `2.42.1` to `2.42.2`; subsequent main CI passed Pester 5 and the full release path.
- 2026-08-25 stable release: main run `32799586405` passed Pester 5, cppcheck, memcheck, all six native variants, cross-variant comparison, packaging, and release completeness. Published full release `v2.42.2` has 27 assets; packages and source archive were built from bumped commit `f0700ffe` (`version.h` 2.42.2). The public tag and stored release target were corrected to that same commit; no local runtime files changed.
- 2026-08-25 release metadata guard: `d6123c32` passes the resolved bumped SHA to both Velopack release creation and release-note generation, with `ReleaseWorkflow.Tests.ps1` covering both calls. `beta` and `main` are aligned at `d6123c32`; run `32801779998` passed Pester 5, cppcheck, and memcheck without creating a duplicate release.
