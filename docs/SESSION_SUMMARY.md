# Session Summary

- 2026-08-29: Main run `33230056711` passed its early gates but skipped build/release because `v2.47.1-beta.1` shares the `2.47.1` header base. Repaired the release-required signal for that beta-to-stable state, with 25 focused Pester 5 passes and an origin-only dry-run proof. Next: fast-forward the repair through beta/main and wait for the replacement full main run; do not manually create a stable release.

- 2026-08-29: Published user-approved `v2.47.1-beta.1` prerelease from `4ce8efdf` after green run `33220187263`. All six channel packages, universal Setup, source archive, checksums, and generated notes are present (32 assets); `Publish-Release.ps1` reports complete. `main` remains untouched. Next: beta feedback only unless the user explicitly approves promotion.

- 2026-08-28 user-approved beta push: `origin/beta` fast-forwarded `e9fadd7a..f5f0ebde` after the user reports the installed Clarity-menu build looks good. It includes the theme grouping, gallery/test coverage, and install record. `main`, releases, installers, engine, and audio services are unchanged. Await beta CI before a release or stable promotion.

- 2026-08-28 user-approved Clarity-menu test install: backed up Program Files AVX-512 `Editor.exe` (`1069728D7657A75EF4F4BEB37DAB56D1345B6659CF8FA610E619D330EFFE7147`) at `artifacts\install-backups\clarity-menu-avx512-20260828-185900\Editor.exe`, then installed and hash-verified local `aaca996` Editor (`6A5255E70E0B0485D26869E30A23842E58D4757081220C45ACD61CBA055A2B59`; v2.47.1.0). Only Editor changed; no engine/config/plug-in/audio-service, remote, or release change. Manual test: check the grouped Interface menu and apply both Clarity themes.

- 2026-08-28 local-only Clarity menu: grouped `Clarity High Contrast` and `Graphite Clarity` together in View > Interface, with stable roster shortcut ordinals. EditorLogic 4,572, Release AVX-512 Editor, Clarity/Graphite dark/light menu gallery, and 12-shot Legacy readability gallery pass. No push, release, installer, or installed-runtime change; next is explicit user direction.

- 2026-08-28 beta test install: green beta run `33214676823` tested commit `2d91bd6d` and produced the AVX-512 artifact. With user approval, backed up the 75-file Program Files runtime at `artifacts\install-backups\beta-v2471-avx512-20260828-182654`, paused/restored Windows Audio, overlaid and hash-verified all 64 v2.47.1 files, and opened Editor. `main` and releases remain unchanged. Next gate: user runtime testing.

- 2026-08-28 beta promotion: user approved fast-forwarding `origin/beta` from `0523964a` to `242a3f86`, the validated v2.47.1 integration. `main`, release state, installers, and installed runtime are unchanged. Next gate: beta CI; do not release or promote to `main` until it is green.

- 2026-08-28 v2.47.1 integration checkpoint: isolated branch `codex/upstream-2471-integration` merged local `beta` into `upstream/main` `df26f9da` as local commit `e94458fc`. No beta/main remote, release, installer, or active runtime changed. Green core/UI evidence: EditorLogic 4,548; HybridConv 1,635; EngineOrchestration 1,259; Release Editor; Device Selector Release; core EqualizerAPO Release; Theme Lab 44/0; full theme switch 132/198/worst 873 ms; skin-module 5/22; Legacy Clarity/Graphite 12-shot gallery; VST-aware 6,600-shot gallery. VST preview ownership: selected endpoint -> fork worker, no endpoint -> upstream panel monitor, Bertom native pop-out -> neither. Theme Lab one-shot exit now avoids offscreen Qt teardown hangs. AudioRegression needs missing reference fixtures; script Pester requires CI/Pester 5; Installer build needs separate user approval. Resume with the Installer compile only if approved, then user-test live VST preview/Bertom/themes before asking for beta promotion.

- 2026-08-28 upstream audit before beta push: `upstream/main` advanced to `v2.47.1` (`df26f9da`). Current prepared local beta (`ef85c9be`) and upstream split at `v2.42.2` (`5524cf25`), with 138 fork-only versus 41 upstream-only commits and 44 overlapping paths. A non-checkout merge preflight found 18 conflicts in shared VST panel/picker, theme, test, translation, changelog, and version files. Upstream's panel preview/monitor and skin readability work overlap behavior this fork already carries. Do not push beta until the user approves a fresh v2.47.1 integration and full revalidation. The audit made no working-file, branch, remote, install, or release change.

- 2026-08-27 Graphite Clarity: added a separate built-in charcoal high-readability theme beside Clarity, with a dark grey (not black) dark mode and clear light companion. It uses a dense square instrument form—wide rails, wireframe badges, gradient scope bars, no stripe, and taller rows—while preserving high-visibility labels, controls, focus, and value-backed knobs in Modern and Legacy Rows. Contrast is mechanically checked on normal, hover, and selected card surfaces; Graphite appends to preserve prior Ctrl+Alt shortcuts. Legacy VST3 fill controls wrap into three-cell lines, removing the gallery's channel-label overlap. The Legacy Rows regression gallery covers both Clarity and Graphite in dark/light active/disabled state. The roster/contrast test was red first, then `EditorLogicTests` passed 4,484 checks. Release AVX-512 Editor, `--theme-lab-test`, and fresh Modern/Legacy offscreen galleries pass; representative Graphite captures were checked. The user-approved Editor-only AVX-512 overlay is installed at `C:\Program Files\EqualizerAPO-XT-x64-avx512\current\Editor.exe`, SHA-256 `B528D825C13789A781ECE675AE7B642A6D1758765CCF4487919DE2828948165D`. The prior Clarity-only Editor (`D28EEB7F098A2C16A66E6FF5383484E6C6AF9CFD83641D9B40DC771AE7FFD287`) is backed up at `artifacts\install-backups\graphite-clarity-avx512-20260827-232558\Editor.exe`; engine, config, plug-ins, and audio services remain untouched. The user reports the candidate seems to work. The beta update is prepared locally after refreshing `origin/beta`, and a future `main` promotion would calculate `2.43.0`; no beta push or release has occurred.

- 2026-08-27 Clarity High Contrast: added built-in `clarity` for both Modern and Legacy Rows with a dark/light pair, maximum legibility tokens, clear focus/selection states, and thicker knobs plus numeric values. Requirement review found Legacy Preamp still used a platform `QDial`; it is now the shared bipolar `AudioKnob`, and CI owns a dedicated required Clarity Legacy gallery. The new roster/contrast regression first failed, then passed in `EditorLogicTests` (4,391 checks). Release Editor build, `--theme-lab-test`, and representative fresh Modern/Legacy gallery screenshots pass. User-approved Editor-only overlay `D28EEB7F098A2C16A66E6FF5383484E6C6AF9CFD83641D9B40DC771AE7FFD287` is installed in the Program Files AVX-512 runtime; backup `artifacts\install-backups\clarity-high-contrast-avx512-20260827-150828\Editor.exe` keeps prior SHA-256 `3210E445FCC2FA44D3EBA6B0C4F25EAD20E159E7563AB47BA7FDC42614FCCAC3`. Engine, configuration, plug-ins, and audio services are unchanged. Next gate: user test the four combinations before beta push.

- 2026-08-27 branch workflow: `beta` is fast-forwarded to the v2.42.5 line. User policy: develop/test on `beta`; promote only user-approved beta work to `main` for stable release. Do not send ordinary work directly to `main`. The durable guide is `docs/BranchWorkflow.md`, linked from the README.

- 2026-08-27 stable release: `v2.42.5` is published from `4b723927514c3df28d1108fd9c22dc1c76cc9fca` with 33 assets. Run `33029195723` passed all six variants, Pester, cppcheck, memcheck, cross-variant comparison, packaging, and release completeness. The release contains the normal popup live VST-preview restoration and Program Files installer repairs. Local branch fast-forwarded to the CI bump commit; no new runtime install occurred.

- 2026-08-26 live-preview runtime result: user reports the installed AVX-512 Editor restores normal separate-panel live VST preview. The Bertom Denoiser Classic native-popup exception remains unchanged and was not newly re-tested.

- 2026-08-26 live-preview candidate installed: after the user closed Editor, overlaid only `C:\Program Files\EqualizerAPO-XT-x64-avx512\current\Editor.exe`. Previous SHA-256 `02AD1CB1840AA96E90EB25EA171B80E72577048FD7326C22E3093691FE727B1C` is backed up at `artifacts\install-backups\live-preview-popup-avx512-20260826-204754\Editor.exe`; installed SHA-256 `8185116EFFEEB26B9B472550A6FCFD914172AF73ED5E7630A0D92BDB8717CF54` is verified. Engine, configuration, plug-ins, and audio services are unchanged. Next gate: a normal VST `Open panel` responds to the current microphone in both UI modes; Bertom still opens/closes without crashing.

- 2026-08-26 live VST-preview repair: the earlier Denoiser popup stability fix globally changed separate native VST panels to run without the microphone/analyzer feed. `VSTPopupLivePreviewPolicy` now restores the feed for normal `Open panel` plug-ins in both LegacyRows and modern cards, while retaining the narrow `Bertom_DenoiserClassic.vst3` exception that prevents its native-panel crash. The focused regression test was red at 1/4318 and green at 4319 checks. AVX-512 Editor 2.42.4.0 candidate SHA-256 `8185116EFFEEB26B9B472550A6FCFD914172AF73ED5E7630A0D92BDB8717CF54` is built, not installed. Next gate: live microphone response in a normal panel and safe Bertom open/close.

- 2026-08-26 local test candidate: rebuilt `Installer\Release\EqualizerAPO-XT-Setup.exe` from the fallback repair. It reports 2.42.4.0, SHA-256 `4B39DF838B213F639759C3767561B20DF3DB26C0D410F8BB8BC729E4E2C1CCBB`, and safe `--detect-only` exits 0. It has not been run to install anything; the next gate is explicit user-approved live Setup testing.

- 2026-08-26 universal Setup x86 repair: the screenshot's Program Files error occurs before downloading because `FOLDERID_ProgramFilesX64` returns `0x80070002` to this x86 front door, even though native x64 resolves `C:\Program Files`. The new fallback reads only the protected 64-bit `ProgramFilesDir` registry value, not an environment variable. The resolver regression, x86 registry probe, Win32 Installer Release build, and EditorLogicTests 4313 pass. Existing AppData and legacy installations remain untouched; package and manual Setup test are next.

- 2026-08-26 installer scope: v2.42.4’s AppData install was caused by `Install-ReleaseBuild.ps1` selecting the legacy per-user channel Setup EXE. The published universal Setup and the released MSI contain the correct machine-install path/property behavior. The helper now defaults to the MSI; per-user selection is explicit for audio CI repro jobs. Machine target is `C:\Program Files\EqualizerAPO-XT-<channel>`. Verified released MSI tables, exact release-asset selection/checksums, Installer build, and EditorLogicTests 4310. No installed files were removed or overwritten; next gate is a user-approved MSI test.

- 2026-08-25 Denoiser Classic popup-panel repair: the optional live analyzer worker called process on the same VST3 instance used by a pop-out controller panel, reproducing an access violation inside Bertom Denoiser Classic. It is now restricted to embedded panels; native pop-outs prioritize stability. `--selftest-vst3-panel` passes the exact raw module through both LegacyRows and modern-card actions. Release Editor build, EditorLogic 4310, and HybridConv 1635 pass. User-approved Editor-only build is installed; configuration, engine, and plug-ins are unchanged. On 2026-08-26, the user confirmed the panel opens/closes in both LegacyRows and modern cards.

- 2026-08-25: published user-approved `v2.42.3-beta.1` from `28d868ae` after green run `32857546525` (Pester 5, cppcheck, memcheck, all six variants, UI gates, cross-variant comparison). The 27-asset prerelease has the beta-pinned universal Setup, six system-wide MSIs/update feeds/full packages, source archive, and SHA256SUMS; the repository completeness gate passes. The Setup clean-rebuild/detect-only check resolves this AVX-512 machine to the tag-pinned beta MSI. No runtime install occurred. Next: manually verify it installs only under `C:\Program Files\EqualizerAPO-XT\<channel>` and preserves existing legacy/per-user installs.

- 2026-08-24: user confirms FabFilter Pro-Q works after the library-load fix. Removed temporary diagnostic probes/debug hook. The first shared split-menu repair on modern VST Browse and LegacyRows Options was visibly too large, so it is now 8px transparent with a 6px chevron; the focused test failed with 120 assertions before the correction and passes at EditorLogicTests 4283. Legacy VST status colours remain tokenized. Clean AVX-512 Editor hash `6B24EFD87C122F405F37DD31312D5681A4FB8FE7F0ADE89E869B21510C095210`; forced-offscreen gallery crashes locally. After the user closes the running test Editor, install it and manually check Copper Forge/Legacy Bronze in both modes.
- 2026-08-24: after the user closed Editor, backed up the prior test binary at `artifacts\install-backups\compact-menu-arrow-avx512-20260824-192222\Editor.exe` and installed only the compact-arrow `Editor.exe` into the active per-user AVX-512 runtime. Old/new hashes are `F13921F032A5BCE8DA90A197944E1F1900E4EC20E65EF0023E219502219FE22E` / `6B24EFD87C122F405F37DD31312D5681A4FB8FE7F0ADE89E869B21510C095210`. No engine or audio-service files changed; manual visual acceptance remains open.
- Upstream refreshed to `ff9c1747614546b68418a5376d0e5f893babd130` (v2.39.0).
- `beta` versus upstream direct merge trial: 149 conflicts; abandoned without changes.
- Current local worktree branch: `codex/upstream-289-port`, based on current upstream.
- Ported VST analyzer preview, microphone capture sources, selected endpoint selection, and row-scoped `Device:` selection.
- Ported VST2 transport-time/process-level safety; the new host test failed before the fix and passed after it.
- Ported block-paced VST preview processing; its worker joins before plug-in shutdown.
- Ported saved custom-theme persistence, JSON import/export, token derivation, Theme Lab UI, temporary live preview, and saved-theme application against the five current upstream base skins.
- Review fixes keep previews transient, keep the Dark menu synchronized to saved-theme state, and preserve Rack/Matrix toolbar chrome through a separate base-renderer identity.
- Ported Midnight Console, Arctic Bloom, Ember Rack, Violet Pulse, and Solar Paper; each retains its own token/id identity while delegating to the matching upstream QSS and painter grammar.
- Ported Obsidian Glass, Aurora Veil, Copper Forge, Neon Nebula, and Noir Chrome with the same token-variant contract; no new skin grammar or DeviceSelector-specific painter was added.
- DeviceSelector resolves all token variants to the same base painter grammar and now restores saved custom-theme tokens/QSS through a shared application path; an isolated qmake build and all 93 expected offscreen shots passed.
- Ported LegacyRows theming without changing legacy row factories, filters, or the stock font engine: shared chrome, dialogs, row paint, and token consumers now follow the selected palette.
- Added Legacy Slate, Blue, Forest, Bronze, and Plum; all use Minimal/Precision grammar for modern/DeviceSelector presentation and a compact heritage QSS/palette layer for legacy rows.
- Preserved upstream VST3 bus-layout path while resolving the port.
- Audited the remaining fork-only feature groups: VST3 layouts, card focus, and VST readability were already carried by current upstream; direct VST3 bundle import was the remaining substantive slice.
- Ported direct VST3 bundle import: the VST card now offers a dedicated `.vst3` folder picker that preserves the active reference if a normal folder is rejected; the importer scans each regular file, requires the host-platform module, rejects reparse/traversal paths, stages the tree inside the target config root, and replaces an old bundle without leaving stale members. The confirmation UI now distinguishes items from file count and surfaces nonfatal cleanup warnings.
- Included VST2 no-native-editor panel safety (`767f3fdf`), so an editor-less VST2 plug-in is rejected before panel opcodes.
- Restored signed/leading-decimal legacy VST parameter parsing and the Visual Studio environment casing/clean-artifact packaging safeguards. The focused VST parser suite now passes 123 checks inside the 1635-check native suite.
- Repaired upstream's duplicate `AudioEngineAccess.cpp` compile entry in `EditorLogicTests.vcxproj`; it caused a parallel-object race. The configured suite now builds and passes 3415 checks.
- Pushed the validated candidate to `origin/codex/upstream-289-port`; this is not an upstream merge or an installed build.
- Reproduced the skin-module CI gate's 76 false failures: token variants are intentional roster entries that reuse one of five `paintBaseId` painter modules. The gate now validates module ownership through `paintBaseId` and passes locally for 5 base modules across 20 themes.
- Reproduced the AVX2 skin-gallery failure locally: Studio-derived `midnight` and `obsidian` were incorrectly required to scroll because the assertion used their selected id instead of the active painter base. The gallery now uses `SkinManager::baseSkinId()`; its stderr is captured by CI and the rebuilt 20-theme gallery passes all 5,840 shots.
- Validation: Editor Release build, offscreen `Editor.exe --selftest-vst`, offscreen 120-cycle `Editor.exe --skin-switch-test`, `EditorLogicTests.exe` (3415 checks, including VST3 picker classification/state preservation, tree copy/replacement/traversal, non-VST3-directory rejection, and preservation of an existing bundle from invalid input), `HybridConvTests.exe` (1635 checks), a 30-shot LegacyRows gallery, and 123 DeviceSelector shots (including a temporary saved-theme probe) passed. Translation catalogs compiled. Representative LegacyRows and DeviceSelector captures were visually checked.
- The temporary desktop self-test can stall; headless Qt mode is the validated local self-test path. No installed files were changed for that runtime setup.
- VST3 repair, 2026-08-24: strict `IPluginFactory3::setHostContext` handling rejected real FabFilter/iZotope factories that validly return `kNotImplemented`. The narrow compatibility change accepts that result only; the added fixture failed before the change (1/102 VST3 host checks) and passes after it (103 VST3 host / 1635 native checks). The repaired loader passes 79/80 installed VST3s; the sole remaining old RNNoise bundle is blocked by its filesystem ACL, while the configured newer RNNoise bundle passes.
- A hash-verified AVX-512 two-file test overlay (`Editor.exe`, `EqualizerAPO.dll`) is installed in the registered `EqualizerAPO-XT-x64-avx512\\current` runtime, with originals backed up under `artifacts\\install-backups`. Config and Program Files were not changed. The new Editor VST self-test timed out at 30 seconds and is not passing proof.
- FabFilter follow-up: Pro-Q 4's `.dll` is a raw VST3 module. The repaired loader passes its library, factory/component/controller initialization, and 2-in/2-out setup. The prior test shortcuts pointed to a stale sandbox-profile runtime, while the normal Program Files Configuration Editor remains the baseline build. The two named `EQ APO XT AVX-512` shortcuts now target the registered AVX-512 test runtime.
- LegacyRows follow-up: `CompactToolBar` capped its vertical actions at the compact `sizeHint`, which displayed Add but clipped Remove/Edit. The minimal fix reserves the base toolbar height as both minimum and maximum. A headless red/green probe confirms all three controls visible. The rebuilt, hash-verified Editor-only test overlay is installed with originals backed up under `artifacts\\install-backups`.
- Next: use the repaired `EQ APO XT AVX-512` shortcut to verify Pro-Q 4 loads and a disposable LegacyRows row can be removed. Then complete the manually dispatched CI build (Pester 5) and remaining VST2/Theme/VST3-bundle/LegacyRows acceptance before beta promotion.
- Theme pass: 20 themes × two modes pass 4.5:1 text/muted/selection; shared token tooltip covers modern + LegacyRows.
- Theme Lab: picker/reset/repair, neutral control preview/audit, and safe saved-theme fallback. Editor Release, EditorLogic 4043, and `--theme-lab-test` passed.
- Installed commit `9f12df1c` Editor-only overlay in the AVX-512 test runtime; backup `artifacts/install-backups/pre-theme-lab-9f12df1c-20260824-135508`; VST DLL + Program Files unchanged.
- Runtime diagnosis, 2026-08-24: the exact configured FabFilter Pro-Q 4 raw VST3 module again logged "could not be loaded". The active engine is a different v2.42.2 `EqualizerAPO.dll` (`52E96A...`), so the Editor-only theme overlay left the loader path unchanged.
- Rebuilt the loader-containing `Common` library and `EqualizerAPO.dll` at AVX-512 from `9f12df1c`; candidate v2.39.0 SHA-256 `3A11AB586EE7D5A2C3A6A3D87FC3744F1E501AEC82F48369FC9FA5B04A8949B7`. AVX-512 `HybridConvTests` passes 1,635 checks, including 103 VST3 host checks. User-approved install backed up the preflight v2.42.2 engine at `artifacts/install-backups/fabfilter-engine-avx512-20260824-144041`, restarted Windows Audio, and confirmed the new `audiodg` process loaded the candidate hash. Next gate: repeat the exact Pro-Q 4 load.
- FabFilter Editor-side repair: the continued generic error was logged by `Editor.exe` PID 49932, not `audiodg` PID 39572. The installed Editor was built before `9f12df1c`; AVX-512 replacement `C54ECB8BAD1074FC113163191767768A2D2A1F9341BD1C2144A6191DD4CFF8A8` is installed, with prior binary backed up at `artifacts/install-backups/fabfilter-editor-avx512-20260824-151810`. It passes `--diagnose`; the exact FabFilter factory probe confirms `setHostContext` returns valid `kNotImplemented`. Qt graphical test flags crash in the agent session for both old/new binaries and are not proof. Next gate: use the named AVX-512 shortcut to add/load/open Pro-Q 4 and verify audio.
- Correction: the log field is `GetCurrentThreadId`, so the generic failure is in the Editor analysis thread rather than a separately inferred process. Exact `VSTPluginLibrary` load/unload passes 12 cycles and a non-GUI temporary Editor FilterEngine probe loads the real config and Pro-Q 4. Temporary tagged diagnostic Editor `BCB4A18EBE8DA00933C5A2336BF3416D4B6FE5FC3F2C4F9922AA9A7B813EE26B` is installed; backup `artifacts/install-backups/fabfilter-editor-diagnostic-avx512-20260824-165026`. Retry UI; if error persists, read tagged log gate then remove instrumentation.
- Next: with approval, install and compare LegacyRows + a modern theme in both modes; test tooltip and custom save/apply/import/export.
- 2026-08-24 v2.42.2 catch-up: exact tag `5524cf25449c83d36a09e42afd119d49712943e6` is merged locally. Fork VST preview/safety and theme work were retained with upstream VST3 slot-fill controls. The only build integration repair was the SkinGallery VST constructor's preview-endpoint placeholder. Release Editor build, EditorLogicTests (4306), HybridConvTests (1635), and EngineOrchestrationTests (1259) pass. No v2.42.2 runtime files are installed or pushed; next gate is an approved manual test install.
- v2.42.2 AVX-512 candidate is ready but not installed: Editor `38E60FBD419364EC2E54FC1DF44EA1C720287FC439D823420557A33F6A7622CB`, EqualizerAPO DLL `F5A474AE444F3AF01E5CD439C8CBF11CB40C82418EC411B065817C22C15974CE`; fresh AVX-512 HybridConvTests passes 1635 checks.
- User approved the v2.42.2 test install. Backed up the active two-file AVX-512 overlay at `artifacts\\install-backups\\v2422-avx512-20260824-202257`, stopped/restored Windows Audio, overlaid only Editor + EqualizerAPO DLL, and hash-verified both. Fresh `audiodg` has not yet loaded the APO, so do not treat this as live-audio proof; user must exercise the configured endpoint and plug-ins before any push.
- 2026-08-24 beta reconciliation: ported beta-only VST3 full-bus/readback negotiation, legacy numeric `Input`/`Output` parser compatibility, and capture channel-mask selection onto the tested v2.42.2 candidate. AVX-512 builds pass; HybridConvTests 1,635 (VST3 host 124; parser 165) and EngineOrchestrationTests 1,259 pass. User reported the installed candidate works and authorized beta promotion. Prior beta history is retained through merge `76717818`, which is published to `origin/beta`. On 2026-08-25 the user also authorized a clean fast-forward to main; no local runtime changes are authorized.
- 2026-08-25 release: GitHub Actions run `32795860636` passed on beta commit `7b2b349c`; release `v2.42.2-beta.1` is published as a prerelease with verified portable AVX2/AVX-512 packages and `SHA256SUMS.txt`. The release is manual-overlay only; it does not change the installed runtime or stable auto-update path.
- 2026-08-25 release retry: the first main workflow was green but skipped build/release because its nearest tag was `v2.42.2-beta.1` and only docs followed it. Added a `Bump-Version.ps1` prerelease-base promotion plus two fixture tests; origin-only replay changes `2.42.1` to `2.42.2` and check mode stays read-only. Subsequent main Pester 5/full matrix/release passed; no local runtime changed.
- 2026-08-25 stable release: main run `32799586405` is green across Pester 5, cppcheck, memcheck, six native variants, UI/package/release gates, and release completeness. Stable `v2.42.2` is public with 27 assets. Its binaries and source archive were built from version commit `f0700ffe`; both the public tag and release target are corrected to that commit. No local runtime files changed.
- Follow-up hardening: `d6123c32` passes the resolved bumped SHA to Velopack creation and release notes, guarded by `ReleaseWorkflow.Tests.ps1`. `beta` and `main` now match at `d6123c32`; run `32801779998` passes Pester 5, cppcheck, and memcheck with no duplicate release.
